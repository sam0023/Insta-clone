import Loader from 'react-loader-spinner'
import './index.css'

const Spinner = () => (
  <Loader
    className="loader"
    type="ThreeDots"
    color="#ffffff"
    height="50"
    width="50"
  />
)
export default Spinner
